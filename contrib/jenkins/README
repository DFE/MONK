This directory contains monk_tf specific file for Jenkins continuous
integration server (see https://jenkins-ci.org/).

  config.xml - configuration for a job running unittests, pylint and
               generating documentation from the dev branch
